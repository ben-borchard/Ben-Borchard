<%= form_for(@article, html: { multipart: true }) do |a| %>

	<div>
		<%= render 'error_messages' %>
	</div>
	
	<div class="form-group">
		<%= a.label :title %><br>
		<%= a.text_field :title, cols: "50" %>
	</div>

	<div class="form-group picture">
		<%= a.file_field :picture, accept: 'image/jpeg,image/gif,image/png' %>
	</div>

	<div class="form-group">
		<%= a.label :text %><br>
		<%= a.text_area :text, size: "140x20",
							   class: "art-body" %>
	</p>

	<p>
		<%= a.submit "Post", class: "btn btn-primary" %>
	</p>

<% end %>